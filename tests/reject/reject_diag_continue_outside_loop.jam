// Ports "Diagnostics - continue outside loop carries :line:".
// expect-error: reject_diag_continue_outside_loop.jam:6: error:
// expect-error: `continue` outside of loop

fn main() i32 {
    continue;
    return 0;
}
