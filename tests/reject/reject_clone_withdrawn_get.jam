// Ported from tests/cpp/test_codegen_errors.cpp "Conditional - withdrawn
// method replays reason at call site" (`get` flavor): Vec(T).get is
// withdrawn when the element is not cloneable; calling it replays the
// reason.
// expect-error: method `std/collections.Vec__Counter.get` is not available for this instantiation
// expect-error: owns resources

const { Vec } = import("std/collections");

const Counter = struct {
    value: u32,
    sink: *mut u32,
};

cfn drop(self: mut Counter) {
    var p: *mut u32 = self.sink;
    p.* = p.* + 1;
}

fn consume(c: move Counter) {
    var owned: Counter = c;
}

fn bad(sink: *mut u32) u32 {
    var v: Vec(Counter) = Vec(Counter).empty();
    v.push(Counter { value: 1, sink: sink });
    var got = v.get(0);
    return 0;
}
fn main() {}
