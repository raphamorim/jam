// Ported from C++ test: "Array - non-comptime size rejected"
// (tests/cpp/test_codegen_errors.cpp testNonComptimeSizeRejected, case 1/2)
// An undefined name in the size position can't fold — rejected with the
// comptime-known diagnostic, not silently sized.
// expect-error: array length must be comptime-known
fn main() {
    var a: [BOGUS]u8 = [0; 4];
}
